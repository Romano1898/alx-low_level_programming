#include "main.h"

/**
* _islower - Checks if a character is lowercase
*
* Return: 1 (Character is lowercase, 0 if otherwise)
* @c: Character to be checked
*/

int _islower(int c)

if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
