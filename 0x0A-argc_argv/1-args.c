#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc - Argument count
 * @argv - Argument vector
 *
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d", argc);
return (0);
}
