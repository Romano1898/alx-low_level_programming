#include "main.h"

/**
 * _strlen - determines length of string
 * @s: stores a single character of the string
 * Return: length of string
 */

int _strlen(char *s)
{
char []str = [100];
[]str = *s;
int i = 0;

while (*s != '\0')
{
  i++;
}
return (i);
}
